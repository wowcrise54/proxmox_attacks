[all:vars]
ansible_user=ubuntu
ansible_ssh_common_args='-o StrictHostKeyChecking=no'
ansible_python_interpreter=/usr/bin/python3

# Management Infrastructure
[management]
mgmt-container ansible_host=10.100.1.10 ansible_connection=local

# Docker Infrastructure
[docker_hosts]
docker-host-01 ansible_host=10.100.2.101

# Kubernetes Infrastructure  
[kubernetes_masters]
k8s-master-01 ansible_host=10.100.2.111

[kubernetes_workers]
k8s-worker-01 ansible_host=10.100.2.121

# Service Groups
[kubernetes:children]
kubernetes_masters
kubernetes_workers

[container_infrastructure:children]
docker_hosts
kubernetes

[infrastructure:children]
management
container_infrastructure

# Network Groups by Subnet
[management_network]
mgmt-container

[infrastructure_network] 
docker-host-01
k8s-master-01
k8s-worker-01

[services_network]
# Add service VMs here when created

# Role-based Groups
[monitoring]
# Add monitoring nodes

[storage]
# Add storage nodes

[web_servers]
# Add web server nodes

[database_servers]
# Add database nodes

# Environment Groups
[development:children]
infrastructure

[staging:children]
# Add staging nodes when created

[production:children]
# Add production nodes when created